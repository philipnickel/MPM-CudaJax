# @package _global_
# Constant active PPC (~8.5), particle count up: same N as particle_count, G derived per-N.
# 5 log-spaced points 1M..25M; the full N catalogue lives in conf/scale/weak_*.yaml.
hydra:
    sweeper:
        params:
            scale: weak_1m,weak_2m,weak_5m,weak_10m,weak_25m
tag: sweep_weak_scaling
