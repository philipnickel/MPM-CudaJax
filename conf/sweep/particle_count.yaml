# @package _global_
# Constant grid (G=96), particle count up.
# 5 log-spaced points 1M..25M; the full N catalogue lives in conf/scale/count_*.yaml.
hydra:
    sweeper:
        params:
            scale: count_1m,count_2m,count_5m,count_10m,count_25m
tag: sweep_particle_count
