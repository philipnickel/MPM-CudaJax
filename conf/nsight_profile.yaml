defaults:
  - config
  - override /sim: benchmark
  - _self_

render:
  enabled: false

nsight:
  target: p2g
  write_json: true
  sweep: null
  configs: null
  plot:
    enabled: false
    filename: nsight_plot.png
  analyze:
    runs: 1
    replay_mode: range
    metric_preset: time
    derive_metric: throughput
