# @package _global_
# Scaling axis: fixed N=20M, growing grid resolution (particle density down).
# Single source of the swept grid list + fixed N, shared by the timing and NCU
# wrappers.
hydra:
    sweeper:
        params:
            sim.num_grids: 64,84,112,148,196

sim:
    n_particles: 20000000
