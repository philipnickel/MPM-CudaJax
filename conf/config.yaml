defaults:
  - backend: jax
  - material: jelly
  - sim: default
  - kernel: jax
  - _self_

output_dir: ./output
tag: jelly
benchmark: false  # skip GIF rendering and frame saving when true
profile: false    # enable CUDA profiler markers (run with: nsys profile --capture-range=cudaProfilerApi ...)
