# @package _global_
# Weak scaling; _self_-first keeps axis order.
defaults:
  - _self_
  - /scaling: weak

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      backend: cuda_v1,cuda_v2,cuda_v3,CuTile
