# @package _global_
# Fixed point, swept by CUDA MPS thread %.
defaults:
    - /scaling: sm
    - _self_

hydra:
    mode: MULTIRUN
    sweeper:
        params:
            backend: cuda_v1,cuda_v2,cuda_v3,CuTile
    sweep:
        dir: outputs/nsight/${gpu_kind:}/sm_scaling
        subdir: ${hydra.job.num}_${hydra.job.override_dirname}
    callbacks:
        nsight_plot:
            scale_axis: mps_thread_percent
