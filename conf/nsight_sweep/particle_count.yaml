# @package _global_
# Fixed grid, growing N; _self_-first keeps axis order.
defaults:
  - _self_
  - /scaling: particle_count

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      backend: cuda_v1,cuda_v2,cuda_v3,CuTile
