# Dedicated XProf trace run (short benchmark capture)
defaults:
    - config
    - override /sim: benchmark
    - _self_

render:
    enabled: false

profile:
    enabled: true

sim:
    steps_per_frame: 3
    num_frames: 2
